# Lean dependency set for running obsidian-iris-mcp's stdio MCP server inside
# the Odysseus container.
#
# Odysseus already installs shared deps such as mcp, httpx, beautifulsoup4,
# pypdf, icalendar, caldav, cryptography, and python-dateutil. Keep this list
# to Iris-specific extras that are needed either at MCP import time or for
# common vault tools. Voice-only deps (faster-whisper, edge-tts, lingua) are
# intentionally left out; those tools fail gracefully if called without them.
ddgs>=9.0
python-docx>=1.0
openpyxl>=3.1
recurring-ical-events>=3.0
matplotlib>=3.7
tzdata>=2025.1
